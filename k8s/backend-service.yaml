apiVersion: v1
kind: Service
metadata:
  name: chatapp-backend-service
  namespace: chatapp-service
spec:
  selector:
    app: chatapp-backend
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30501
  type: NodePort
